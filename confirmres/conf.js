document.querySelector('form').addEventListener('submit', function (e) {
    e.preventDefault();
    // Simuler une redirection
    window.location.href = 'confirmation.html'; // Remplacez par l'URL de la page de confirmation
});
